# Processed Data

This directory contains datasets generated during data preparation and merging.

These files correspond to the intermediate and analytical datasets used by the R scripts in the `code/` directory.
